machine:
  timezone:
    America/New_York
dependencies:
  pre:
    - gem install bundler
  bundler:
    without: [production]
deployment:
  staging:
    branch: master
    heroku:
      appname: starter-app-rails5